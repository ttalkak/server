spring:
  config:
    import: optional:configserver:http://localhost:8888
    name: auth-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

hosts:
  logstash: localhost